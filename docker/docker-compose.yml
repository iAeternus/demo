version: "3.9"
services:
  redis:
    image: redis:7
    container_name: demo-redis
    ports:
      - "6379:6379"
    command: [ "redis-server", "--appendonly", "yes" ]
    volumes:
      - redisdata:/data

volumes:
  redisdata: